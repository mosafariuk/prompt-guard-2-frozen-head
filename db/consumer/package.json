{
  "name": "threat-log-consumer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Queue consumer: drains threat-log events and batch-inserts them into partitioned PostgreSQL. Off the request path (paper Section VI-B).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
